name: api-template-dpr-now #nama aplikasi yang akan dibuat di cloudfoundry
path: target\coba_API-0.0.1-SNAPSHOT.war #target snapshot  di folder coba_API
memory: 1G
services:
  - "mysqldb" #nama services yang sudah dibuat di cloudfoundry